document.addEventListener('DOMContentLoaded', function() { fetch('inventory.csv') .then(response => response.text()) .then(data => { const rows = data.split('\n').map(row => row.split(',')); const tableBody = document.querySelector('#inventory tbody'); rows.forEach((row, index) => { if (index > 0) { const tr = document.createElement('tr'); row.forEach(cell => { const td = document.createElement('td'); td.textContent = cell; tr.appendChild(td); }); tableBody.appendChild(tr); } }); }) .catch(error => { alert('Error loading inventory data.'); }); document.querySelector('#inventory-filter').addEventListener('input', function() { const filter = this.value.toLowerCase(); const rows = document.querySelectorAll('#inventory tbody tr'); rows.forEach(row => { const text = row.textContent.toLowerCase(); row.style.display = text.includes(filter) ? '' : 'none'; }); }); }); function viewImage(src) { const viewer = document.getElementById('large-image'); viewer.src = src; viewer.style.display = 'block'; }